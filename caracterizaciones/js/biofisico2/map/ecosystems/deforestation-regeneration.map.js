export const deforestationRegenerationMap = {
    id: "deforestacion",
    group: "ECOSISTEMAS",
    strategy: "single-layer",
    supports: config => config?.id === "deforestacion" || config?.id === "bosque_depto"
};
