# Cabeceras de seguridad (formato Netlify / Cloudflare Pages).
# Para nginx / Apache / IIS u otros servidores, configurarlas a nivel de servidor:
# ver docs/despliegue/README_DESPLIEGUE_FRONTEND.md (sección 4).
#
# CSP (H-04 / H-17): orígenes FIJOS por directiva; se eliminó el comodín `https:` de
# script-src / style-src / font-src / connect-src / frame-src (antes permitía cargar
# scripts desde CUALQUIER host https → vector de XSS).
# script-src NO lleva 'unsafe-inline': 0 <script> embebidos y 0 eval()/new Function.
#
# Estilos — tres directivas en vez de una, a proposito:
#   style-src-elem 'self' <cdn>   → bloques <style> y <link>. SIN 'unsafe-inline': es
#                                   donde vive el riesgo real. Todo el CSS embebido del
#                                   repo se extrajo a archivos (oot-auth-guard.css,
#                                   caracterizaciones/css/inline-*.css, limites-*.css).
#   style-src-attr 'unsafe-inline' → atributos style="..." sueltos. Los necesitan dos
#                                   librerias de terceros (jquery.imagemapster y
#                                   jquery.connections en colombia-ot/ruta/) para
#                                   posicionar elementos. Un atributo style NO ejecuta
#                                   JavaScript, asi que el riesgo residual es bajo;
#                                   parchear librerias de terceros minificadas lo era mas.
#   style-src  ... 'unsafe-inline' → SOLO como respaldo para navegadores que no entienden
#                                   -elem/-attr. Donde ambas existen, ganan las especificas.
#
# ATENCION AL VERIFICAR: el grep de esta afirmacion debe cubrir <style>…</style>,
# document.createElement('style') y atributos style=. La revision de 2026-07-19 solo
# busco atributos y dio por limpios 3 bloques <style> y 3 <style> creados por JS.
# `https:` se mantiene SOLO en img-src: los basemaps ráster (OSM) provienen de varios
# hosts y las imágenes no ejecutan código.
#
# Orígenes externos vigentes:
#   scripts/estilos CDN → cdnjs, jsdelivr, unpkg, ajax.googleapis (jQuery), gstatic
#   analítica GA4       → googletagmanager (script) + *.google-analytics (beacon)
#   fuentes             → fonts.googleapis (css) + fonts.gstatic (woff) + pro.fontawesome
#   mapas ArcGIS        → js.arcgis.com (librería + CSS) y, para las peticiones fetch/XHR
#                          reales de tiles/datos (WebGL, no <img>, por eso van en connect-src
#                          y no en img-src pese a que img-src ya permite https: en general):
#                          services.arcgisonline.com (World_Imagery), tiles.arcgis.com y
#                          services2.arcgis.com (capas propias de IGAC). Solo usado en
#                          caracterizaciones/, cargue/, colombia-ot/pot/ y colombia-ot/ruta/.
#   mapas MapLibre       → Modulo_Determinantes.html / Modulo_Indicadores.html usan
#   (Determinantes/      → MapLibre GL JS (unpkg.com, ya cubierto) con basemap real de
#    Indicadores)          OpenStreetMap: tile.openstreetmap.org/{z}/{x}/{y}.png — también
#                          via fetch (WebGL), va en connect-src. Ninguno de los dos sistemas
#                          de mapas del sitio (ArcGIS en Caracterizaciones, OSM/MapLibre en
#                          Determinantes/Indicadores) tenía su dominio de tiles en la CSP —
#                          el basemap cargaba en blanco en ambos. Encontrado probando en vivo
#                          2026-07-19, nunca se había probado con la CSP realmente aplicada.
#   backend/API + cargue→ 'self' y *.igac.gov.co (incl. serviciosgeovisor:8080)
#                          + localhost:8001 / 127.0.0.1:8001 — el backend en DESARROLLO.
#                          Tras el split de repos el frontend se sirve aparte (:5500), asi
#                          que el backend ya no es 'self' y sin este origen el navegador
#                          bloquea todo /api/* en local. No amplia nada en produccion: un
#                          `localhost` solo resuelve a la maquina de quien visita, y esta
#                          politica no lleva 'unsafe-inline' en script-src, asi que no hay
#                          por donde inyectar codigo que aproveche ese permiso. Quitarlo el
#                          dia que el despliegue institucional sirva frontend y backend
#                          desde el mismo origen.
#   auth (Firebase SDK) → identitytoolkit.googleapis.com + securetoken.googleapis.com
#                          (el SDK los llama por XHR para el intercambio de tokens;
#                          sin esto signInWithPopup falla tras elegir la cuenta de Google)
#   source maps (.map)  → los mismos hosts de script-src/style-src (cdnjs, jsdelivr, unpkg,
#                          gstatic) deben repetirse en connect-src: con DevTools abierto el
#                          navegador pide el .map de cada script/css para depurar, y esa
#                          petición se evalúa contra connect-src, no contra script-src/style-src.
#                          Descubierto probando el login real 2026-07-19 (ruido en consola que
#                          tapaba los errores reales).
#   iframes Colombia OT → www.colombiaot.gov.co
#   auth (Firebase)     → geovisor-igac.firebaseapp.com (iframe oculto que usa
#                          signInWithPopup para el handshake; sin esto el login falla
#                          en silencio — el popup de Google abre pero nunca confirma sesión)
#                          + colombiaot2.firebaseapp.com, el proyecto de DESARROLLO que
#                          `config.js` usa cuando el host es localhost. Va aquí porque el
#                          <meta> CSP sí se aplica en local (las cabeceras de este archivo
#                          no): sin él, en la máquina de desarrollo el popup abre y nunca
#                          confirma, exactamente el mismo fallo mudo de arriba. Se listan
#                          los dos dominios en vez de un comodín *.firebaseapp.com para no
#                          ampliar la política de producción más de lo necesario.
#                          + apis.google.com (gapi, helper interno del SDK de Firebase Auth
#                          para el mismo flujo — algunas versiones del SDK lo cargan también)
#   PMTiles/tiles       → los sirve el backend por HTTP Range, cubierto por *.igac.gov.co
#                          en connect-src (OOT_TILES_BASE hereda de OOT_API_BASE).
#
# DESPLIEGUE ACTUAL — GitHub Pages NO emite cabeceras personalizadas, asi que este archivo
# (formato Netlify/Cloudflare Pages) no se aplica alli. La misma politica se inyecta como
# <meta http-equiv> en cada HTML con `python scripts/aplicar_csp.py`, que LEE ESTE ARCHIVO:
# es la unica fuente de verdad, no hay dos politicas que puedan divergir. En <meta> el
# navegador ignora frame-ancestors (y report-uri/sandbox); el resto si aplica. Cuando el
# despliegue pase a nginx/IIS, traducir esta linea a la config del servidor y dejar la
# <meta>: cabecera y meta se combinan por interseccion, y salen del mismo sitio.
# Nota despliegue: si el backend/API o los tiles se sirven desde OTRO dominio (p. ej.
# un túnel de desarrollo o un object storage), añadir ese origen a connect-src aquí.
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: SAMEORIGIN
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=(), usb=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Content-Security-Policy: default-src 'self'; script-src 'self' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net https://unpkg.com https://ajax.googleapis.com https://www.googletagmanager.com https://www.gstatic.com https://apis.google.com https://js.arcgis.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net https://unpkg.com https://fonts.googleapis.com https://pro.fontawesome.com https://www.gstatic.com https://js.arcgis.com; style-src-elem 'self' https://cdnjs.cloudflare.com https://cdn.jsdelivr.net https://unpkg.com https://fonts.googleapis.com https://pro.fontawesome.com https://www.gstatic.com https://js.arcgis.com; style-src-attr 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com https://pro.fontawesome.com https://js.arcgis.com; connect-src 'self' https://*.trycloudflare.com http://localhost:8001 http://127.0.0.1:8001 https://*.igac.gov.co https://serviciosgeovisor.igac.gov.co:8080 https://identitytoolkit.googleapis.com https://securetoken.googleapis.com https://www.googleapis.com https://www.googletagmanager.com https://*.google-analytics.com https://*.analytics.google.com https://cdnjs.cloudflare.com https://cdn.jsdelivr.net https://unpkg.com https://www.gstatic.com https://js.arcgis.com https://services.arcgisonline.com https://tiles.arcgis.com https://services2.arcgis.com https://tile.openstreetmap.org; worker-src 'self' blob: https://js.arcgis.com; child-src 'self' blob:; frame-src 'self' https://www.colombiaot.gov.co https://geovisor-igac.firebaseapp.com https://colombiaot2.firebaseapp.com https://apis.google.com; object-src 'none'; base-uri 'self'; frame-ancestors 'self'

